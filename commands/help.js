module.exports = {
    default: {
        op:false,
        description:"wait what?",
        execute: function(context) {
            context.msg.channel.send("nobody can hear you, but you should do e>account register");
        }
    }
}